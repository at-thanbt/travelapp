<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asiantech.travelapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Enter your GoogleMap Android API Key -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/google_api_key" />

        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainTourGuideActivity" />
        <activity android:name=".activities.DetailTourActivity" />
        <activity android:name=".activities.LoginTourRistActivity" />
        <activity android:name=".activities.LoginTourGuideActivity" />
        <activity android:name=".activities.RuleActivity" />
        <activity android:name=".activities.AddTourristActivity" />
        <activity android:name=".activities.AddScheduleActivity" />
        <activity android:name=".activities.ChatActivity" />
        <activity android:name=".activities.AddTourActivity" />

    </application>

</manifest>